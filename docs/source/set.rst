
Setting Defaults
================

The Lager Client allows you to set certain default options for running other commands,
to simplify the commands needed to run and prevent minor option typos from slowing you down.

.. click:: lager_cli.setter.commands:setter
   :prog: lager set
   :nested: full
